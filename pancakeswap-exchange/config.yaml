# yaml-language-server: $schema=./node_modules/envio/evm.schema.json
name: pancakeswap-exchange
description: PancakeSwap exchange-v2 subgraph (BSC) ported to HyperIndex
rollback_on_reorg: false
contracts:
  - name: Factory
    events:
      # subgraph: PairCreated(indexed address,indexed address,address,uint256)
      # (the 4th uint256 param is unnamed in the Factory ABI; named here for codegen, unused)
      - event: "PairCreated(address indexed token0, address indexed token1, address pair, uint256 pairIndex)"
  # Subgraph template: Pair (registered dynamically on PairCreated)
  - name: Pair
    events:
      - event: "Mint(address indexed sender, uint256 amount0, uint256 amount1)"
        field_selection:
          transaction_fields: [hash]
      - event: "Burn(address indexed sender, uint256 amount0, uint256 amount1, address indexed to)"
        field_selection:
          transaction_fields: [hash]
      - event: "Swap(address indexed sender, uint256 amount0In, uint256 amount1In, uint256 amount0Out, uint256 amount1Out, address indexed to)"
        field_selection:
          transaction_fields: [hash, from]
      - event: "Transfer(address indexed from, address indexed to, uint256 value)"
        field_selection:
          transaction_fields: [hash]
      - event: "Sync(uint112 reserve0, uint112 reserve1)"
chains:
  - id: 56
    start_block: 6809737
    # end_block: 6859737   # uncomment for bounded validation runs (see validation.json)
    contracts:
      - name: Factory
        address: "0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73"
      # dynamically registered template (no static address)
      - name: Pair
